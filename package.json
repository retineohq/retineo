{
  "name": "echo-core",
  "version": "0.1.0",
  "description": "ECHO Core — Content Compilation Engine (L0-L3)",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src/**/*.ts",
    "test": "vitest"
  },
  "dependencies": {
    "zod": "^3.23.0",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "eslint": "^8.57.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
